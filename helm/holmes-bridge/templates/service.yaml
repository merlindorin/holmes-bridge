apiVersion: v1
kind: Service
metadata:
  name: {{ include "holmes-bridge.fullname" . }}
  labels:
    {{- include "holmes-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "holmes-bridge.selectorLabels" . | nindent 4 }}
